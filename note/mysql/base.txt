1. 主键，外键，唯一键，复合唯一键

2.乐观锁
    Sequelize通过模型实例版本计数内置支持乐观锁定。乐观锁定在默认情况下是禁用的，
    可以在特定的模型定义或全局模型配置中通过将version属性设置为true来启用它。更多细节请参见模型配置。
    乐观锁定允许并发访问用于编辑的模型记录，
    并防止覆盖数据的冲突。它通过检查自读取记录以来，其他进程是否对记录进行了更改，
    并在检测到冲突时抛出OptimisticLockError来实现这一点。
，锁表