const scheme = require('./scheme');

module.exports = {
    id: scheme.id,
    url: scheme.url,
    title: scheme.title,
    description: scheme.description,
    keyword: scheme.keyword,
    tagId: scheme.tagId
};